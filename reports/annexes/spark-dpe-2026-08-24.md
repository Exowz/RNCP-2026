# Preuve Spark DPE — 24 aout 2026

Commande executee, sans acces Internet :

```bash
source scripts/spark-env.sh
.venv/bin/python -m pytest tests/data/test_collecte_dpe_spark.py -q
```

Resultat : `1 passed` en 7,4 s. Le test a demarre Spark 3.5.9 en `local[1]`,
lu `data/samples/dpe_ademe_sample.csv`, restitue 726 lignes et verifie les
colonnes ADEME `N°DPE`, `Code_INSEE_(BAN)` et `id_parcelle_rapprochee`.

La JVM par defaut (OpenJDK 26) echouait avant lecture avec
`UnsupportedOperationException: getSubject is not supported` dans Hadoop. Le
script epingle OpenJDK 17 local ; ce diagnostic et cette correction sont
reproductibles et seront ajoutes a la configuration CI.
